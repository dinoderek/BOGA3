# Hosted environment placeholders for follow-on M5 tasks (`T-20260220-09+`).
# Do not commit real hosted secrets.
# Machine setup: `./scripts/boga-config-init.sh` copies this to ~/.config/boga/supabase/env.hosted.
# Worktree setup: `./scripts/worktree-setup.sh` symlinks supabase/.env.hosted -> the global copy.

SUPABASE_PROJECT_REF=
SUPABASE_ACCESS_TOKEN=
SUPABASE_DB_PASSWORD=

# Populated after a hosted project exists.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
